# Backend target used by Vite dev proxy
VITE_API_TARGET=http://localhost:8000

# Optional absolute URL for production deployments.
# Leave empty to use same-origin relative URLs (recommended when reverse-proxied).
VITE_API_BASE_URL=

